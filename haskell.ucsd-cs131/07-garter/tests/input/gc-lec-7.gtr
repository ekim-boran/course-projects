def range(i, j):
  if (i < j):
    (i, range(i+1, j))
  else:
    false

def sum(l):
  if l == false:
    0 
  else:
    l[0] + sum(l[1])

let t = let l = range(0, 3) in
        sum(l)  
in 
let l1 = range(t, t + 3)
in
  (1000, l1) 

